[build]
builder = "DOCKERFILE"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[service]
name = "universe-odyssey"

# Note: Railway will automatically inject standard MySQL environment variables
# when you add a MySQL plugin.
